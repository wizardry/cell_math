version: 2
jobs:
  build:
    docker:
      - image: cimg/node:lts
    steps:
      - checkout
      - restore_cache:
          keys:
            - npm-{{ checksum "./app/package.json" }}
      - run: cd ./app npm install
      - save_cache:
          paths:
            - node_modules
          key: npm-{{ checksum "./app/package.json" }}
      - run: cd ./app npm run lint
      - run: cd ./app npm run test
  deploy:
    docker:
      - image: cimg/node:lts
    steps:
      - checkout
      - restore_cache:
          keys:
            - npm-{{ checksum "./app/package.json" }}
      - run: cd ./app
      - run: npm install
      - run: git config user.name $GITHUB_NAME
      - run: git config user.email $GITHUB_EMAIL
      - run: cd ./app npm run generate:gh-pages
      - run: cd ./app npm run deploy:gh-pages
workflows:
  version: 2
  build-deploy:
    jobs:
      - build
      - deploy:
          requires:
            - build
          filters:
            branches:
              only: master
